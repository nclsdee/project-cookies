<div class="container">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2">
      <div id="items">
        <% @items.each do |item| %>
          <h4><%= item.title %> <%= item.price  %></h4>

        <% end %>
      </div>
    </div>
  </div>
</div>
